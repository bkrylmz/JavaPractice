package tasks;

import java.util.Scanner;

public class IndexOfA {
    public static void main(String[] args) {
        Scanner s = new Scanner(System.in);
        String word = s.next();

        /*
        Use `String methods` to find the index of the letter `a` in the given `word`.
        Print the index of the character:

> Index of a: $index number

#### Note: If there is multiple occurrences of the letter `a` we will output
only the index of the first occurrence
         */






    }
}
